#!/bin/zsh
# This file is for local environment variables and can override any of
# the variables in zshenv.  Don't change zshenv unless you have to and
# make sure its not changed with scripts.
export VERIFY_USE_PUBLIC_BINARIES=true

